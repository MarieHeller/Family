<div class="navbar navbar-expand-sm navbar-light d-flex navbar-lewagon fixed-top">

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <% if user_signed_in? %>
  <div class="d-flex space-around align-items-center">
    <%= link_to "/fams", class: "navbar-brand" do %>
      Family Time
    <% end %>


    <%= form_for :search, url: fams_path, method: :get do |f| %>
    <%= f.text_field :location, class: "search-bar" %>
    <%= f.submit "Find Family", class: "btn-search" %>
    <% end %>
  </div>
  <div class="d-flex space-around">
    <div class="nav-link">
      <%= link_to "Become a family", new_fam_path %>
    </div>
    <div class="nav-link">
      <%= link_to "Profile", profile_path(current_user) %>
    </div>
    <div class="nav-link">
      <%= link_to "Log Out", destroy_user_session_path, method: :delete %>
    </div>
  </div>
  <% else %>

    <div>
      <%= link_to "/fams", class: "navbar-brand" do %>
        Family Time
      <% end %>
    </div>
    <div class="d-flex space-around">
    <div class="nav-link">
      <%= link_to "Become a family", new_fam_path %>
    </div>
    <div class="nav-link">
      <%= link_to "Sign Up", new_user_registration_path %>
    </div>
    <div class="nav-link">
      <%= link_to "Log In", new_user_session_path%>
    </div>
    <% end %>
  </div>
</div>

